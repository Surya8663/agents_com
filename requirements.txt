# Phase 3: OCR Dependencies
easyocr==1.7.2

# Phase 4: Agents & LLM (WITH LANGRAPH)
# Core LangChain ecosystem with compatible versions
langgraph==0.0.46
langchain==0.1.0
langchain-core==0.1.23  # Specific compatible version
langchain-community==0.0.10  # OLDER VERSION that works with langsmith<0.1.0
langsmith==0.0.77  # EXACT version that langchain==0.1.0 requires

# Vector Database & Embeddings
qdrant-client==1.7.0
sentence-transformers==2.2.2

# LLM & HTTP
openai==1.12.0  # For openai-compatible APIs
httpx==0.26.0
aiohttp==3.9.3
tenacity==8.2.3
python-dotenv==1.0.0

# Dependencies that sometimes cause issues
protobuf==3.20.3  # Specific version for paddlepaddle compatibility
PyYAML==6.0.2  # Specific version for paddlex compatibility
huggingface-hub==0.16.4  # Older version compatible with sentence-transformers